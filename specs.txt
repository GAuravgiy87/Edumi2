# EduMi2 Application – Required Hardware & Software Specs

## �hardware Requirements
- **CPU**: 4‑core x86_64 (Intel i5 / AMD Ryzen 5 or better).  Video‑processing services (LiveKit, ffmpeg, video_editing, live_stream) benefit from additional cores.
- **Memory (RAM)**: Minimum **8 GB**.  For concurrent video streams or heavy meeting usage, **16 GB** is recommended.
- **Storage**:
  - **Base application code & Docker images**: ~5 GB.
  - **PostgreSQL data volume**: Allocate **≥20 GB** (more if you retain recordings).
  - **Video assets / recordings**: Allocate additional space (e.g., 100 GB) depending on expected usage.
- **GPU (optional but highly recommended)**: Modern NVIDIA GPU with CUDA support (e.g., GTX 1660 or better) for accelerated video encoding/decoding in `video_editing` and `live_stream` services.
- **Network**: Stable broadband; ports **8002‑8007**, **6379**, **5432**, **7880‑7881**, **443** (HTTPS) must be reachable between containers (Docker bridge network handles this).

## 🛠️ Software Stack
| Component | Version | Installation Note |
|-----------|---------|-------------------|
| **Operating System** | Windows 11 (Docker Desktop) **or** any modern Linux distro | Docker Desktop for Windows or Docker Engine on Linux |
| **Docker Engine** | ≥ 20.10 | Install Docker Desktop (Windows) or `apt-get install docker.io` (Linux) |
| **Docker‑Compose** | v2 (bundled with Docker Desktop) | Use `docker compose` CLI |
| **Python** | 3.11 | Use official installer or `pyenv`/`conda` |
| **pip** | latest (`python -m pip install --upgrade pip`) |
| **PostgreSQL** | 15 (Docker image `postgres:15-alpine`) | Data persisted via host volume `./postgres_data` |
| **Redis** | 7 (Docker image `redis:7-alpine`) |
| **LiveKit Server** | latest (`livekit/livekit-server:latest`) |
| **Nginx** | stable‑alpine (optional reverse‑proxy) |
| **FFmpeg** | ≥ 5.0 | Required by video services; installed in Dockerfile (`apt-get install -y ffmpeg`) |
| **Python Packages** (see `requirements.txt`) |
|- Django ≥ 4.2 |
|- daphne |
|- channels |
|- psycopg2‑binary ≥ 2.9 |
|- redis |
|- livekit‑server‑client |
|- celery |
|- any other app‑specific libs |
| **Git** | optional for source control |

## 📦 Installing the Stack (quick guide)
1. **Docker Desktop** – download from https://www.docker.com/products/docker-desktop and follow the installer.
2. **Clone the repository** (if not already local):
   ```powershell
   git clone <repo‑url> "C:\Users\hp\Desktop\Edumi2-my-work2"
   ```
3. **Python (local dev only)** – `python -m venv venv && .\venv\Scripts\activate && pip install -r requirements.txt`
4. **Build & launch containers**:
   ```powershell
   cd C:\Users\hp\Desktop\Edumi2-my-work2
   docker compose build
   docker compose up -d   # starts db, redis, livekit, all micro‑services, nginx, celery
   ```
5. **Verify** – `docker compose ps` should show all services **Up**.  Check DB: `docker exec -it edumi_db psql -U edumi_user -d edumi_db -c "\dt"`.
6. **Persisted data** – PostgreSQL data lives in `./postgres_data`; do not delete this folder.
7. **Optional** – Place TLS certificates in `certs/` and configure `nginx/conf.d/` for HTTPS.

## 📝 Notes
- All containers share the same `.env` file, which already points to Docker service names (`db`, `redis`, `livekit`).
- If you run the app **without Docker**, install PostgreSQL and Redis locally and set `DATABASE_URL` and `REDIS_URL` accordingly.
- For GPU‑accelerated video processing on Linux, expose the GPU to the container (e.g., `--gpus all` in the compose file) and install NVIDIA drivers on the host.
- Regularly back‑up `./postgres_data` and any `./staticfiles`/`./media` directories.
